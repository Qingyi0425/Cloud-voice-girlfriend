# 云端语音AI：快速使用

## 1. 准备环境

安装 Python 3.11 或更高版本，在本目录打开 PowerShell 后执行：

```powershell
python -m venv .venv
.venv\Scripts\Activate.ps1
pip install -r requirements.txt
```

如果 PowerShell 不允许激活环境，直接使用：

```powershell
.venv\Scripts\python.exe -m pip install -r requirements.txt
```

## 2. 填写接口配置

将 `.env.example` 重命名为 `.env`，填写至少两组必填配置：

- `LLM_BASE_URL`、`LLM_API_KEY`、`LLM_MODEL`：你的 OpenAI 兼容大模型中转站。
- `DOUBAO_TTS_BASE_URL`、`DOUBAO_TTS_API_KEY`、`DOUBAO_TTS_RESOURCE_ID`、`DOUBAO_TTS_VOICE_ID`：你的豆包 TTS 配置。

也可以先启动服务，再在网页右上角的设置面板填写并保存。`.env` 含密钥，不能分享或上传到代码仓库。

浏览器的语音输入使用浏览器自带语音转文字，通常不需要配置 STT；建议使用 Chrome 或 Edge。无法使用时可直接输入文字。

## 3. 启动与访问

```powershell
python -m app
```

打开 <http://127.0.0.1:8001>，输入文字或使用语音输入即可试听。

## 4. 修改角色与记忆

直接编辑根目录的 Markdown 文件即可，下一次聊天会自动读取。常用文件：

- `SOUL.md`：核心人格。
- `STYLE.md`：表达方式和示例。
- `USER.md`：用户资料。
- `MEMORY.md`：明确确认的长期记忆。

不要把 `.env`、聊天记录或个人资料分享给他人。
